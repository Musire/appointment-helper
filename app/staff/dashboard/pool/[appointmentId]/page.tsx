import { PoolDetails } from "@/domains/appointments/components/pool";

export default function PoolAppointmentDetails () {
    return (
        <div className="flex flex-1  overflow-y-hidden">
            <PoolDetails />
        </div>
    );
}